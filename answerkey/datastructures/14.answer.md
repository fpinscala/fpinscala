```scala
/*
`append` simply replaces the `MyNil` constructor of the first list with the second list, which is exactly the operation
performed by `foldRight`.
*/
def appendViaFoldRight[A](l: MyList[A], r: MyList[A]): MyList[A] =
  foldRight(l, r, Cons(_,_))
```
